---
type: project_topic
status: archived
summary: "2026-09-08 早期阶段的规则与技能增量修复快照；后续同步审查结论见 skill-sync-contract.md。"
tags: [skills, rules, audit]
contains: [decision, lesson]
created: "2026-09-08"
updated: "2026-09-08"
related: [windows-environment-compatibility-and-agent-onboarding.md, skill-sync-contract.md]
authoring_mode: ai_generated
---
# 规则与技能增量修复

本页保留 2026-09-08 早期阶段的事实和教训，其中“本轮”“后续”“下一轮”均指该阶段。当时暂缓的 Git 来源和 Skills Manager 审查现已继续，当前同步行为和验证范围以 [同步契约](skill-sync-contract.md) 为准。

## 当时的结论

- 先以原审计快照比较当前实际源，再按文件 SHA-256 和联接解析结果核验；已经修好的内容保留，只补剩余缺陷。写入前保存原字节，逐文件再次比较并读回。
- 规则改为先报告策略变化再继续授权范围内工作；重复失败时重审假设，仅撤销本轮拥有且已证明无效的修改。临时配置恢复与批准保留的持久修复分开。
- 技能补齐实际缺失的参考，修复别名的宿主专用工具假设，限定持续教学与任务记录的范围。Cairn 模板明确技能参考的路径基准，部分升级不能被记录为全部完成，外部知识写入前准备可审阅稿并核对具体授权。
- 旧交接保留日期和历史身份。目录联接共享同一实际文件，不能把联接别名当成独立副本写路由；项目源文件归属与全局可发现范围分别核对。
- 当前项目目录是本轮实际编辑位置，不代表已决定多机 Git 来源。用户要求 Skills Manager、同步链路和多处副本来源留待下一轮；本轮未应用同步/安装脚本草案、未调整挂载、未执行 Git 提交或推送。

## 验证与边界

已对修改后的正文检查 YAML、必要的本地引用与文件差异，应用后逐文件读回比对。诊断包装器通过隔离 mock 检查失败阻断与退出码传递；没有重新验证酒店设备状态。原文件和恢复清单保存在仓库 `backups/` 对应本轮时间目录；完整改动清单在本次 Codex 任务的 outputs 中。

## 教训：测试隔离必须覆盖间接进程

同步脚本的首轮基线测试虽然模拟了 Git 和挂载脚本，并使用临时用户目录，仍从 PATH 找到真实 Skills Manager CLI，发生意外 `skills sync` 调用。实际仓库的数据库和锁文件修改时间随之变化；没有原始数据库备份，不能判定内部改动或保证所有挂载均未受影响。没有进行盲目回滚。

后续同步测试已停止。本次不是同步工具验收；下轮先只读核对数据库中的来源、实际路径、联接/副本和 Git 状态。未来对包装器测试必须同时遮蔽 PATH 命令解析及备用绝对路径，并对测试目录外的写入建立检测，不能只模拟主命令就宣称离线。
